{
  "name": "webapp-lit-characters",
  "version": "1.0.0",
  "description": "This is a repository for project web component characters with lit element",
  "main": "index.js",
  "scripts": {
    "start": "web-dev-server --node-resolve --open --watch",
    "start:fallback": "web-dev-server --node-resolve --app-index index.html --open --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vaadin/router": "^1.7.5",
    "lit": "^2.8.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.3.1"
  }
}
